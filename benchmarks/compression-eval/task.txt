为这个仓库的 src/ 目录写一份完整的架构总览文档。

要求：
1. 用 read_file / directory_tree / grep_search 等工具，**逐个**读取 src/ 下每个子目录（cli/, mcp/, memory/, telemetry/, transcript/, tools/, ui/, i18n/, loop/ 等）的代表性文件。每个子目录至少读 3-5 个核心 .ts 文件。
2. 不要只 grep 关键词就下结论 —— 必须打开文件读真实内容，从代码里抽出"这个模块的职责 / 它的核心数据结构 / 它对外暴露什么"。
3. 边读边在心里搭建依赖图。
4. 全部读完后，输出一份 markdown 报告，包含：
   - 整体架构图（mermaid 或 ASCII，模块之间的依赖关系）
   - 按模块列出：职责 / 关键文件 / 对外接口 / 内部依赖
   - 至少 8 个具体的代码引用（路径:行号），证明你真读了
   - 结尾给出"如果新人加入项目，应该按什么顺序读代码"的建议

这是一个真实的代码审计任务，请不要偷懒、不要瞎编、不要用通用模板。文件读得不够多就直接说"我读得不够"。
